# Bar Monitoring System - Python Dependencies

# Hailo Integration
# NOTE: Hailo software must be installed separately via:
# sudo apt install hailo-all
# Then clone hailo-rpi5-examples and run ./install.sh

# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
PyYAML>=6.0

# GStreamer Python bindings (for Hailo pipeline)
PyGObject>=3.42.0

# Database
# sqlite3 is built into Python

# 🔥 STOLEN LIBRARIES (replacing custom code)
# Supervision - 18k stars - line crossing, dwell tracking, zones
supervision>=0.16.0

# Streamlit - 33k stars - beautiful dashboards
streamlit>=1.28.0

# Sensors (will add in next phase)
# Adafruit-DHT>=1.4.0        # Temperature sensor
# adafruit-circuitpython-bh1750  # Lux sensor
# pyaudio>=0.2.11            # Audio input
# pybluez>=0.23              # Bluetooth scanning

# Web interface (keeping Flask as backup)
flask>=2.0.0
flask-cors>=3.0.10

# Utilities
python-dateutil>=2.8.0
pandas>=1.5.0  # For Streamlit charts

# API integrations (for Toast POS)
requests>=2.28.0
